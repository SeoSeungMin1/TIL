## 배운 내용

- CI/CD 큰 틀, 순서
    - EC2 직접 배포(빅뱅 배포)
        - 개발자가 직접 EC2에 접속해 코드를 내려받고 서버를 실행하는 가장 기본적인 방식
        - 구조는 단순하지만 사람이 직접 배포하므로 휴먼 에러 발생하기 쉽고, 배포 중 서비스가 중단
        - 서버의 언어 버전이나 라이브러리 차이로 인해 환경 불일치 문제 발생
    - Docker + Nginx 배포(단일 인스턴스)
        - 애플리케이션과 실행 환경을 Docker 이미지로 포장하고, EC2는 이미지를 받아 컨테이너를 실행
        - 실행 환경까지 함께 포함되므로 어디서 실행하든 동일한 환경 보장
        - 이미지 생성과 배포는 여전히 사람이 직접 수행
    - Docker + Compose + Nginx(단일 인스턴스)
        - 여러 컨테이너의 실행 방법을 docker-compose.yml 파일 하나로 관리
        - 실행 구성을 코드로 관리하여 재현성과 운영 편의성을 높임
        - 배포를 시작하는 과정은 아직 사람이 직접 수행
    - CI/CD 파이프라인 구축(단일 인스턴스)
        - 코드 Push를 시작으로 빌드, 테스트, 이미지 생성, 배포까지 자동으로 수행
        - 반복 작업을 자동화하여 생산성과 일관성, 추적 가능성을 높임.
        - 단일 인스턴스에서는 기존 컨테이너를 교체해야 하므로 짧은 서비스 중단 발생
    - 무중단 배포 - Blue/Green(다중 인스턴스)
        - 기존 환경(Blue)은 유지한 채 새로운 환경(Green)에 먼저 배포하고, 정상 동작을 확인한 뒤 트래픽을 전환하는 방식
        - 서비스 중단 없이 배포할 수 있으며, 문제가 생기면 기존 환경으로 빠르게 롤백 가능
        - 두 개의 운영 환경을 유지해야 하므로 비용과 운영 복잡도가 증가
    
    → 배포 기술은 환경 일관성 확보 → 구성 관리 → 배포 자동화 → 무중단 운영으로 발전해 왔으며, 서비스 규모와 운영 환경에 맞는 방식을 선택하는 것이 중요.

- DevOps
    - 개발과 운영을 협업으로 연결하여 빠르고 안정적으로 소프트웨어를 배포하기 위한 문화와 방법론
    - 특정 프로그램이나 도구가 아니라 개발과 운영이 함께 일하는 방식
    - 사용 이유
        - 개발과 운영의 협업 강화
        - 배포 속도 향상
        - 서비스 안정성 향상
        - 자동화를 통한 생산성 향상
    - 개발 영역(Development)
        - 새로운 기능을 설계하고 구현하는 영역
        - 기능을 테스트하고 빌드하여 배포 가능한 상태까지 만드는 역할
        - 변경 사항을 자주 통합하고 검증하여 안정적인 결과물을 준비
    - 운영 영역(Operations)
        - 개발된 애플리케이션을 실제 서비스 환경에 배포하는 영역
        - 서버, 네트워크 등 인프라를 구축하고 관리
        - 서비스 운영 상태를 모니터링하고 장애 발생 시 대응
        - 안정적인 서비스 운영을 유지하는 역할
    - IaC(Infrastructure as Code)
        - 인프라 구성을 코드로 관리하는 방식
        - 서버, 네트워크 등의 설정을 코드로 작성하여 동일한 환경을 반복해서 생성 가능
        - 사용 이유
            - 사람의 수작업 및 실수 감소
            - 동일한 환경을 언제든 재현 가능
            - Git을 통한 버전 관리 및 변경 이력 추적 가능
    - 기존 개발/운영 분리 방식의 문제
        - 개발팀은 기능을 빠르게 배포하려고 함
        - 운영팀은 서비스 안정성을 우선시함
        - 서로의 목표가 달라 배포가 지연될 수 있음
        - 수작업 배포 과정에서 휴먼 에러 발생 가능
        - 장애 발생 시 코드 문제인지 운영 환경 문제인지 원인 파악 어려움
        - 복구 시간이 길어지고 서비스 품질 저하로 이어질 수 있음
    - DevOps가 해결하는 것
        - 개발과 운영이 공동의 목표를 가지고 협업
        - 빌드, 테스트, 배포 과정을 자동화하여 사람의 개입 최소화
        - 반복 작업을 줄여 일관성과 재현성 확보
        - 빠른 배포와 안정적인 운영을 동시에 달성
    - CI/CD와의 관계
        - DevOps를 실행하기 위한 대표적인 자동화 방법
        - CI  : 코드 변경 사항을 지속적으로 통합하고 빌드 및 테스트 수행
        - CD : 검증된 결과물을 실제 운영 환경까지 자동으로 배포
        - DevOps의 핵심 목표인 빠르고 안정적인 배포를 가능하게 하는 자동화
- AWS EC2
    - AWS에서 가상 서버를 생성하고 관리할 수 있도록 제공하는 클라우드 컴퓨팅 서비스
    - 물리 서버를 직접 구매하지 않고 인터넷을 통해 필요한 만큼 서버를 생성하여 사용 가능
    - EC2에서 생성되는 가상 서버 한 대를 인스턴스라고 함
    - 사용 이유
        - 필요한 시점에 즉시 서버를 생성하고 확장 가능
        - 사용한 만큼만 비용 지불하여 비용 효율적
        - CPU, 메모리, GPU 등 목적에 맞는 다양한 인스턴스 타입을 선택 가능
        - AWS의 IAM, VPC, Security Group 등 다른 서비스와 함께 사용할 수 있다.
    - 인스턴스
        - EC2를 통해 생성된 하나의 가상 서버
        - CPU, 메모리, 저장소, 운영체제를 갖춘 하나의 컴퓨터
        - 사용자가 배포한 애플리케이션이 실행되고 사용자 요청을 처리하는 운영 환경
    - EC2
        - Elastic : 트래픽에 따라 인스턴스를 늘리거나 줄일 수 있는 확장성
        - Compute : CPU, 메모리 등 컴퓨팅 자원을 제공하는 가상 컴퓨터
        - Cloud : 인터넷을 통해 필요한 순간에 생성하고 사용한 만큼 비용을 지불하는 클라우드 환경
    - 워크로드
        - 서버나 애플리케이션이 실제로 처리해야 하는 작업의 종류와 양
        - 웹 서버, 데이터베이스, AI 연산 등 목적에 맞는 인스턴스를 선택하여 실행할 수 있다.
    - EC2 인스턴스 수명 주기
        - 시작
            - 인스턴스를 생성하고 초기 설정을 수행하는 단계
            - 생성되는 순간부터 비용이 발생
        - 실행
            - 애플리케이션이 실행되어 사용자 요청을 처리하는 정상 운영 상태
        - 중지
            - 인스턴스를 일시적으로 중단한 상태
            - 컴퓨팅 비용은 멈추지만 EBS 저장소 비용은 계속 발생
        - 종료
            - 인스턴스를 완전히 삭제하는 단계
            - 설정에 따라 EBS도 함께 삭제하거나 별도로 유지
    - EBS(Elastic Block Store)
        - EC2 인스턴스에 연결하여 사용하는 가상 저장소
        - 인스턴스가 컴퓨터 본체라면 EBS는 하드디스크 역할
        - 인스턴스를 중지해도 EBS는 유지되며 저장소 비용은 계속 발생
    - 퍼블릭 IP
        - 인터넷에서 EC2에 접속하기 위한 공인 IP 주소
        - 인스턴스를 중지 후 다시 실행하면 기본적으로 새로운 퍼블릭 IP가 할당
        - 주소를 계속 유지하려면 Elastic IP 사용
    - 온프레미스
        - 클라우드가 아닌 조직 내부의 물리 서버에서 직접 시스템을 운영하는 방식
        - 서버 구매, 설치, 유지보수를 직접 수행
- IAM
    - AWS 리소스에 대한 사용자 인증과 권한을 관리하는 서비스
    - AWS에서 누가 어떤 작업을 수행할 수 있는지 결정
    - AWS 계정 전체에 적용되는 글로벌 서비스이며, 특정 리전(AWS가 데이터센터를 운영하는 하나의 지역 단위)이나 VPC에 속하지 않는다.
    - 사용 이유
        - 사용자별로 필요한 권한만 부여하여 보안을 강화
        - 실수나 오용으로 인한 리소스 변경 및 삭제 방지
        - 역할과 정책을 이용해 권한을 체계적으로 관리
        - AWS 서비스 간에도 안전하게 권한을 위임하기 위해
    - 역할
        - AWS API 요청이 실제 서비스에서 처리되기 전에 허용할지 거부할지를 판단하는 관문
        - AWS 리소스 자체를 관리하는 것이 아니라 AWS API 작업에 대한 권한을 관리
        - IAM은 권한, VPC는 네트워크, Security Group은 트래픽 담당
    - 주요 구성 요소
        - 사용자
            - AWS 리소스에 접근하는 개별 사용자(신원)
            - 일반적으로 사람 한 명당 하나의 계정을 사용
        - 그룹
            - 여러 사용자를 묶어 동일한 권한을 부여하는 집합
            - 권한 변경 시 그룹만 수정하면 되어 관리가 편리
        - 역할
            - 사람이나 AWS 서비스가 필요한 순간에만 임시로 사용하는 권한 묶음
            - 임시 자격 증명이 발급되고 일정 시간이 지나면 자동으로 만료
            - 주로 EC2, Lambda 등 AWS 서비스가 다른 AWS 서비스 호출할 때 사용
        - 정책
            - 실제 권한 내용을 정의한 규칙 문서(JSON)
            - 어떤 AWS 서비스의 어떤 작업을 어떤 대상에 대해 수행할 수 있는지를 정의
            - 사용자, 그룹, 역할에 연결되어 권한 부여
    - 요청 처리 흐름
        1. 사용자 또는 AWS 서비스가 AWS API 호출
        2. IAM이 자격 증명을 확인하여 인증 수행
        3. 연결된 정책(User, Group, Role)을 모두 평가
        4. 허용되면 요청이 실제 AWS 서비스에서 실행
        5. 거부되면 권한 오류 발생
    - 다른 서비스와 차이
    
    | 서비스 | 담당 |
    | --- | --- |
    | **IAM** | 누가 어떤 AWS 작업을 수행할 수 있는가(권한) |
    | **VPC** | EC2가 위치하는 네트워크와 통신 경로 |
    | **Security Group** | 어떤 IP·포트의 네트워크 트래픽을 허용할 것인가 |
- VPC
    - AWS 클라우드 안에서 사용자가 직접 만드는 논리적으로 격리된 가상 네트워크
    - EC2, RDS 등 네트워크를 사용하는 AWS 리소스가 배치되는 공간
    - AWS의 여러 사용자가 같은 물리 인프라를 사용하더라도, 자신만의 독립된 네트워크 환경을 구성
    - 사용 이유
        - 서비스별 네트워크를 분리하여 보안을 강화하기 위해
        - 인터넷에 공개할 리소스와 내부 리소스를 분리하기 위해
        - 트래픽 경로를 제어하여 안전한 네트워크 환경을 구축하기 위해
        - 장애 범위와 보안 범위를 분리하여 안정적인 서비스를 운영하기 위해
    - 역할
        - AWS 리소스가 위치할 네트워크 공간을 제공
        - 리소스 간 통신 경로와 인터넷 연결 여부를 결정
        - 네트워크를 여러 구역으로 나누고, 트래픽 흐름을 제어하여 보안을 강화
    - VPC의 계층 구조
        
        ```
        AWS 계정
           │
           ▼
        서울 리전
           │
           ▼
        VPC
           │
           ├───────────────┐
           ▼               ▼
         AZ-a             AZ-b
           │               │
           ▼               ▼
        Subnet           Subnet
           │               │
           ▼               ▼
        EC2              RDS
        ```
        
        → AWS 계정 → 리전 → VPC → 서브넷 → EC2 순으로 계층이 구성되며, EC2는 특정 리전의 특정 VPC 안의 특정 서브넷(AZ)에 생성되어 실행된다.
        
    - 주요 구성 요소
        - 공간을 나누는 구성 요소
            - VPC
                - 논리적으로 격리된 하나의 가상 네트워크
                - 사용할 IP 주소 범위를 정의
                - 리전 단위
            - 서브넷
                - VPC를 용도별로 나눈 작은 네트워크
                - 퍼블릭/프라이빗으로 구분하여 리소스를 배치
                - 하나의 서브넷은 하나의 가용 영역(AZ)에만 속한다.
        - 경로를 정하는 구성 요소
            - 라우팅 테이블
                - 목적지에 따라 트래픽을 어디로 보낼지 결정하는 규칙 목록
                - 서브넷마다 연결되어 트래픽 경로를 제어
            - 인터넷 게이트웨이(양방향)
                - VPC와 인터넷을 연결하는 출입구
                - 퍼블릭 서브넷이 인터넷과 통신할 수 있도록 한다.
            - NAT 게이트웨이(단방향)
                - 프라이빗 서브넷이 인터넷으로 나가는 통신만 가능하게 한다.
                - 프라이빗 서브넷이 직접 인터넷으로 나가는 것이 아니라 Public Subnet에 있는 NAT Gateway를 거쳐서 나간다.
                - 외부에서 먼저 내부로 접속하는 것은 허용안한다.
                - Public Subnet : NAT Gateway, ALB, Bastion Host
                - Private Subnet : EC2, RDS, Redis
            - NAT 게이트 관점 추가 정보
                - 가능한 방향
                    
                    ```
                                  인터넷
                                     ▲
                                     │
                             Internet Gateway
                                     ▲
                                     │
                    ┌─────────────────────────────────┐
                    │              VPC                │
                    │                                 │
                    │ Public Subnet   Private Subnet  │
                    │ ┌────────────┐  ┌────────────┐  │
                    │ │NAT Gateway │◀─│Private EC2 │  │
                    │ └────────────┘  └────────────┘  │
                    └─────────────────────────────────┘
                    
                    방향 : Private EC2 → NAT Gateway → Internet Gateway → 인터넷
                    ```
                    
                    → Private EC2가 먼저 요청하는 상황
                    
                    ```
                    인터넷
                        │
                        ▼
                    Internet Gateway
                        │
                        ▼
                    NAT Gateway
                        │
                        ▼
                    Private EC2
                    ```
                    
                    → Private EC2가 먼저 보낸 요청에 대한 응답이므로 NAT Gateway → Private EC2 전달 가능
                    
                - 불가능한 방향
                    
                    ```
                                  인터넷
                                     │
                                     ▼
                             Internet Gateway
                                     │
                                     ▼
                    ┌─────────────────────────────────┐
                    │              VPC                │
                    │                                 │
                    │ Public Subnet   Private Subnet  │
                    │ ┌────────────┐  ┌────────────┐  │
                    │ │NAT Gateway │✖▶│Private EC2 │  │
                    │ └────────────┘  └────────────┘  │
                    └─────────────────────────────────┘
                    
                    방향 : 인터넷 → NAT Gateway → Private EC2 ❌
                    ```
                    
        - 트래픽을 검사하는 구성 요소
            - 보안 그룹(Security Group)
                - 인스턴스 단위의 상태 저장(Stateful) 방화벽
                - 어떤 IP와 포트의 연결을 허용할지 제어한다.
            - 네트워크 ACL
                - 서브넷 단위의 상태 비저장(Stateless) 방화벽
                - 허용과 거부 규칙을 모두 설정할 수 있다.
        
    - 퍼블릭 서브넷
        - 인터넷 게이트웨이와 연결되어 외부 인터넷에서 접근 가능한 서브넷
        - 웹 서버, Load Balancer 등 외부 요청을 받아야 하는 리소스를 배치한다.
    - 프라이빗 서브넷
        - 인터넷에서 직접 접근할 수 없는 서브넷
        - DB, Redis 등 내부 서비스만 배치한다.
        - 필요한 경우 NAT Gateway를 통해 외부로만 통신한다.
    - 요청 처리 흐름
        1. 사용자의 요청이 인터넷을 통해 VPC의 인터넷 게이트웨이에 도착한다.
        2. 라우팅 테이블이 목적지 서브넷으로 경로를 결정한다.
        3. 네트워크 ACL이 서브넷 단위에서 허용 여부를 확인한다.
        4. 보안 그룹이 인스턴스 단위에서 허용 여부를 확인한다.
        5. 모든 검사를 통과하면 EC2가 요청을 처리한다.
        6. DB가 필요하면 VPC 내부 네트워크를 통해 프라이빗 서브넷의 DB와 통신한다.
- AWS Security Group
    - EC2, RDS 등 AWS 리소스에 연결되는 인스턴스 단위의 가상 방화벽
    - 어떤 네트워크 트래픽을 허용할지 제어하는 보안 규칙
    - 기본적으로 모든 인바운드는 차단, 모든 아웃바운드는 허용
    - 사용 이유
        - 허용된 사용자만 서버에 접근하도록 하기 위해
        - 불필요한 포트와 IP 접근을 차단하여 보안을 강화하기 위해
        - 인터넷에 공개할 서비스와 내부 서비스의 접근 범위를 구분하기 위해
        - 애플리케이션까지 요청이 도달하기 전에 네트워크 수준에서 차단하기 위해
    - 역할
        - 인스턴스로 들어오는  트래픽을 제어 → 인바운드
        - 인스턴스에서 나가는 트래픽을 제어 → 아웃바운드
        - 허용된 IP와 포트만 통신하도록 제한
        - AWS 리소스의 첫 번째 네트워크 보안 장치 역할
    - Security Group 위치
        
        ```
        인터넷
            │
            ▼
        Internet Gateway
            │
            ▼
        Routing Table
            │
            ▼
        Network ACL
            │
            ▼
        Security Group
            │
            ▼
        EC2
        ```
        
        → Security Group은 인스턴스의 네트워크 인터페이스에 연결되어 있으며, EC2 바로 앞에서 들어오고 나가는 트래픽 검사
        
    - 주요 구성 요소
        - 인바운드
            - 외부 → 인스턴스로 들어오는 요청을 제어
            - 설정 항목 : Source(IP), Protocol, Port
            - 기본값 : 모든 인바운드 차단, 필요한 규칙만 허용
        - 아웃바운드
            - 인스턴스 → 외부로 나가는 요청 제어
            - 기본값 : 모든 아웃바운드 허용
            - 필요하면 특정 목적지만 허용하도록 변경 가능
        - 프로토콜
            - 어떤 방식으로 통신할지 지정
        - 포트
            - 어떤 서비스로 연결할지 지정
            - SSH : 22, HTTP : 80, HTTPS : 443
        - 소스
            - 어디에서 오는 요청을 허용할지 지정
            - 예시
                - 0.0.0.0/0 → 모든 IPv4 허용
                - : : /0 → 모든 IPv6 허용
                - 다른 Security Group
        - Stateful(상태 저장)
            - 인바운드 요청을 허용하면 응답은 자동 허용
            - 아웃바운드 요청을 허용하면 응답도 자동 허용
            - 요청과 응답을 하나의 연결로 관리
            - 응답을 위한 별도의 규칙을 만들 필요 없음
    - 특징
        - 허용 규칙만 설정 가능
        - 거부 규칙은 지원하지 않음
        - 여러 개의 Security Group을 하나의 인스턴스에 연결 가능
        - 하나의 Security Group을 여러 인스턴스에서 함께 사용 가능
        - 규칙을 수정하면 즉시 적용
        - 인스턴스 단위에서 동작하는 가상 방화벽
    - 요청 처리 흐름
        1. 사용자의 요청이 인터넷을 통해 VPC로 들어온다.
        2. 라우팅 테이블이 목적지 서브넷을 결정한다.
        3. Network ACL이 서브넷 단위에서 요청을 검사한다.
        4. Security Group이 IP, 프로토콜, 포트를 확인한다.
        5. 허용된 요청만 EC2로 전달된다.
        6. EC2의 응답은 Stateful 특성으로 인해 자동으로 사용자에게 전달된다.
- Elastic IP
    - AWS에서 제공하는 고정(Public) IP 주소
    - EC2 인스턴스에 직접 속하는 것이 아니라 AWS 계정에 할당되는 리전 단위 자원
    - 인스턴스가 중지되거나 교체되어도 같은 퍼블릭 IP를 계속 사용할 수 있도록 해준다.
    - 할당(Allocate) 과 연결(Associate) 이 분리되어 있어 필요할 때 원하는 인스턴스에 연결할 수 있다.
    - 사용 이유
        - EC2를 중지 후 다시 시작하면 퍼블릭 IP가 변경되기 때문
        - 서버 주소를 고정하여 DNS, 외부 연동, 접근 설정 등을 변경하지 않고 유지하기 위해 사용
        - 인스턴스를 새로 교체하더라도 Elastic IP만 옮기면 기존 주소를 그대로 사용할 수 있다.
    - Elastic IP 위치
        - 인터넷 → Elastic IP → Internet Gateway
    - 주요 개념
        - Allocate(할당)
            - Elastic IP를 AWS 계정에 확보하는 단계
            - 아직 어떤 인스턴스에도 연결되지 않은 상태
        - Associate (연결)
            - 확보한 Elastic IP를 특정 EC2 인스턴스에 연결하는 단계
            - 연결 대상은 언제든 변경할 수 있다.
        - Public IP
            - 인터넷에서 사용할 수 있는 전 세계적으로 고유한 IP 주소
            - EC2의 기본 Public IP는 중지 후 재시작하면 변경될 수 있다.
        - Private IP
            - VPC 내부에서 EC2끼리 통신하기 위한 내부 IP 주소
            - 인터넷에서는 직접 접근할 수 없다.
    - 특징
        - AWS 계정이 소유하는 리전 단위 자원
        - 인스턴스와 독립적으로 관리
        - 할당과 연결이 분리
        - 인스턴스 교체해도 동일한 주소를 사용 가능
        - 주소를 고정할 뿐이며 보안이나 네트워크 접근 권한을 제어하지 않는다.
    - 요청 처리 흐름
        1. 사용자가 Elastic IP로 접속
        2. Internet Gateway를 통해 VPC로 들어온다.
        3. Routing Table이 목적지 서브넷을 결정한다.
        4. Network ACL이 서브넷 단위에서 검사한다.
        5. Security Group이 EC2 단위에서 검사한다.
        6. 허용되면 EC2가 요청을 처리한다.
