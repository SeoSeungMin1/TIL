## 배운 내용

- Route 53
    - AWS에서 제공하는 DNS 서비스
    - 사용자가 입력한 도메인 이름을 실제 서버(IP 주소)나 AWS 리소스 주소로 변환
    - Route 53은 요청을 전달하는 것이 아니라 접속해야 할 주소를 알려주는 역할
    - 53은 DNS가 사용하는 기본 포트 번호
    - DNS 조회 과정
        
        ```
        사용자
           │
           ▼
        리졸버(DNS Resolver)
           │
           ▼
        루트 네임 서버 (Root Name Server)
           │
           ▼
        TLD 네임 서버 (.com, .net, .org, .kr ...)
           │
           ▼
        권한 있는 네임 서버 (Route 53)
           │
           ▼
        Elastic IP / ALB / CloudFront 등의 주소
        ```
        
        → 사용자가 도메인 입력하면 여러 DNS 서버를 거쳐 Route 53이 최종 IP 주소 응답
        
    - 사용 이유
        - IP 대신 도메인으로 서비스에 접속하기 위해
        - 서버 IP가 변경되어도 도메인은 그대로 사용하기 위해
        - AWS 리소스와 도메인을 쉽게 연결하기 위해
    - Route 53 역할
        - 도메인과 IP 주소 연결
        - 우리 도메인에 대한 최종 DNS 응답 제공
        - 서버가 변경되어도 도메인은 그대로 유지 가능
    - 호스팅 영역
        - 하나의 도메인에 대한 DNS 레코드를 저장하는 공간
        - 도메인마다 하나씩 생성
        - DNS 레코드 : 도메인 이름과 실제 연결될 대상(IP 주소, ALB, CloudFront 등)를 적어놓은 설정 정보 → 도메인 연결 정보 적어 놓은 주소록
    
    - Route 53 Resolver
        - VPC 내부에서 발생하는 DNS 조회를 처리하는 기능
        - 온프레미스와 VPC가 함께 사용하는 환경에서 이름 조회를 연결한다.→ 이름을 조회할 수 있도록 중계함
    - 요청 흐름에서의 위치
        - 사용자 → Route 53(DNS) → Internet Gateway → VPC → EC2
        - Route 53은 실제 요청이 서버로 가기 전에 도메인을 IP 주소로 변환하는 가장 앞 단계
- **Reverse Proxy**
    - 클라이언트의 요청을 대신 받아 백엔드 서버로 전달하고, 서버의 응답을 다시 클라이언트에게 반환하는 서버 앞단의 중개 서버
    - 사용자는 실제 서버가 아니라 리버스 프록시와만 통신하며, 실제 서버의 위치와 구조를 알 필요가 없다.
    - Nginx, Apache, Caddy 등이 리버스 프록시 역할을 수행
    - 사용 이유
        - 실제 서버의 주소를 외부에 노출하지 않기 위해
        - 여러 대의 서버로 요청을 분산(로드 밸런싱)하기 위해
        - HTTPS(SSL/TSL) 암호화를 한 곳에서 처리하기 위해
        - 캐싱, 압축 등 공통 기능을 한 곳에서 처리
        - 서버를 추가하거나 교체해도 클라이언트는 동일한 주소(도메인 주소)로 접속할 수 있도록 하기 위해
    - Ngiinx 사용 구조
        
        ```
        사용자
           │
        Route 53 (도메인 → IP)
           │
        인터넷
           │
        Internet Gateway
           │
        VPC
           │
        Nginx (Reverse Proxy)
           │
        Spring Boot
        ```
        
    - ALB 사용 구조
        
        ```
        사용자
           │
        Route 53
           │
        인터넷
           │
        Internet Gateway
           │
        VPC
           │
        ALB (Reverse Proxy)
           │
        Spring Boot
        ```
        
    - 역할
        - 클라이언트의 요청을 가장 먼저 받는다
        - 전달 규칙에 따라 적절한 백엔드 서버로 요청을 전달한다.
        - 서버의 응답을 다시 클라이언트에게 반환
        - 여러 서버를 하나의 진입점으로 관리
        - 공통 기능(HTTPS, 캐싱, 압축, 로드 밸런싱 등)을 중앙에서 처리
    - 요청 처리 흐름
        1. 사용자가 브라우저에 도메인 입력
        2. Route 53(DNS)이 도메인을 IP 주소로 변환
        3. 브라우저가 해당 IP로 HTTP/HTTPS 요청을 보낸다.
        4. Reverse Proxy가 요청을 수신
        5. 설정된 규칙에 따라 적절한 백엔드 서버로 요청 전달
        6. 백엔드 서버가 요청을 처리
        7. 응답을 Reverse Proxy로 반환
        8. Reverse Proxy가 클라이언트에게 최종 응답을 전달
    - 특징
        - 실제 서버를 외부에 노출하지 않는다.
        - 하나의 주소로 여러 서버를 관리 가능
        - 특정 제품이 아니라 역할(구조)를 의미한다. → 서비스가 아니다.
        - Nginx, Apache, Caddy 등이 대표적인 구현체
        - 백엔드 서버 연결에 실패하면 502 Bad Gateway 오류 발생
    - 502 Bad Gateway
        - 리버스 프록시까지 요청이 갔는데 백엔드에 연결하지 못하거나 백엔드 자체가 제대로 된 응답을 반환하지 못한 경우
- ELB
    - AWS에서 제공하는 로드 밸런서 서비스
    - 사용자의 요청을 여러 EC2 인스턴스로 자동 분산하는 서비스
    - 리버스 프록시 역할을 하며, 요청을 받아 적절한 서버로 전달
    - 사용 이유
        - 여러 서버에 요청을 분산하여 부하를 줄이기 위해
        - 특정 서버에 장애가 발생하면 정상 서버로 자동 요청을 전달하기 위해
        - 서버를 추가하거나 제가해도 사용자는 동일한 주소로 접속하기 위해
        - HTTPS(SSL/TLS) 처리와 보안 기능을 한 곳에서 관리하기 위해
    - 역할
        - 사용자 요청을 먼저 받는 단일 진입점
        - 여러 EC2 인스턴스로 요청을 자동 분산
        - 대상 서버의 상태를 주기적으로 확인(Health Check)
        - 장애가 발생한 서버에는 요청을 보내지 않음
        - HTTPS 종료 및 리버스 프록시 역할 수행
    - ELB 위치
        
        ```
        사용자
           │
        Route 53
           │
        인터넷
           │
        Internet Gateway
           │
        VPC
           │
        Public Subnet
           │
        ELB
           │
        Security Group
           │
        EC2
        ```
        
        → Route 53은 ELB의 DNS 이름(Alias 레코드) 반환
        
        → ELB가 요청을 받은 후 적절한 EC2로 전달
        
    - 단일 서버 vs 다중 서버
        
        
        | 구분 | 단일 서버(EC2 1대) | 다중 서버(EC2 여러 대) |
        | --- | --- | --- |
        | 서버 수 | 1대 | 2대 이상 |
        | Route 53 연결 대상 | **Elastic IP** | **ELB(Alias 레코드)** |
        | 접속 경로 | Route 53 → Elastic IP → EC2 | Route 53 → Alias → ELB → 여러 EC2 |
        | 고정 IP 사용 | O (Elastic IP) | X (ELB는 고정 IP 없음) |
        | ELB 사용 | 선택 | 필수(일반적) |
        | 로드 밸런싱 | X | O |
        | 장애 대응 | 서버가 죽으면 서비스 중단 | 정상 서버로 요청 분산 가능 |
        | 확장성 | 낮음 | 높음 |
    - 구성 요소
        - Listener(리스너)
            - ELB가 어떤 프로토콜과 포트로 요청을 받을지 결정
            - 예) HTTP(80), HTTPS(443)
        - Target Group(대상 그룹)
            - 요청을 처리할 EC2 인스턴스들의 그룹
            - ELB는 이 그룹에서 대상 서버를 선택하여 요청을 전달
        - Health Cheack
            - 대상 서버가 정상인지 주기적으로 확인
            - 정상 서버에만 요청을 전달
            - 장애 서버는 자동으로 제외
    - ELB 종류
        - ALB(Application Load Balancer)
            - HTTP/HTTPS 전용
            - URL, Header, Query 등을 기준으로 요청 분기 가능
            - 대부분의 웹 애플리케이션에서 사용
        - NLB(Network Load Balancer)
            - TCP / UDP 수준에서 동작
            - 매우 빠르고 지연 시간이 낮음
            - 대용량 네트워크 트래픽 처리에 적합
    - 로드 밸런싱 방식
        - Round Robin : 서버를 순서대로 균등하게 선택
        - 최소 연결 방식(Least Connection) : 현재 연결 수가 가장 적은 서버 선택
        - 가중치 두는 방식(Weighted) : 성능이 좋은 서버에 더 많은 요청 분산
        - Sticky Session
            - 같은 사용자르 계속 같은 서버로 연결
            - 서버 메모리에 세션을 저장하는 경우 사용하지만, 확장성이 떨어져 잘 사용하지 않는다.
    - Auto Scaling 연동
        - Auto Scaling이 EC2를 추가하면 Target Group에 자동 등록
        - EC2를 제거하면 Target Group에서 자동으로 제외
        - ELB와 함께 사용하면 서버 수가 변경되어도 사용자 접속 주소는 변하지 않는다.
- ACM
    - AWS에서 SSL/TLS 인증서를 생성, 관리, 배포하는 관리형 서비스
    - HTTPS 통신을 위한 인증서를 자동으로 관리
    - 사용 이유
        - HTTPS 통신을 쉽게 적용하기 위해
        - 인증서를 안전하게 관리하고 자동 갱신하기 위해
        - 인증서 EC2에 직접 설치하지 않고 AWS 서비스에 연결하기 위해
    - 역할
        - SSL/TLS 인증서 발급
        - 도메인 소유권 확인
        - 인증서 보관 및 자동 갱신
        - ELB 등 AWS 서비스에 인증서 연결
    - 요청 처리 흐름
        
        ```
        사용자
           │
        Route 53
           │
        ELB
           │
        (ACM 인증서로 TLS 연결)
           │
        EC2
        ```
        
        → 사용자가 HTTPS로 접속하면 ELB가 ACM 인증서를 브라우저에 전달
        
        → 브라우저가 인증서를 신뢰하면 TLS 연결이 생성되고 이후 요청이 EC2로 전달
        
    - SSL/TLS 인증서
        - 서버의 신원을 증명하는 디지털 인증서
        - HTTPS 통신 시 암호화와 서버 신원 확인을 제공
    - 퍼블릭 인증서 vs 프라이빗 인증서
        - 퍼블릭 인증서
            - 공개 인증기관(CA)이 발급
            - 인터넷에 공개된 웹사이트에서 사용
            - 브라우저가 기본적으로 신뢰
        - 프라이빗 인증서
            - 조직 내부 인증기관이 발급
            - 사내 서비스 등 내부 시스템에서 사용
            - 별도의 신뢰 설정 필요
