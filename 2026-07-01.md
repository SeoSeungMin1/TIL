## 배운 내용

- 테스트 더블
    - 테스트 대상(SUT)이 의존하는 실제 객체를 대신하는 가짜 객체를 통칭하는 용어
    - 외부 시스템(DB, API, 파일 등)에 의존하지 않고 테스트할 수 있게 해준다.
    - 관련 용어
        - SUT(System Under Test) : 테스트 대상이 되는 코드
        - 의존 객체 : SUT가 동작하기 위해 사용하는 다른 객체 → Repository, 외부 API Client, 파일 저장소 등
    - 사용 이유
        - 실제 의존성을 그대로 사용하면 테스트가 느려지고 외부 환경에 따라 결과가 달라질 수 있다.
        - 실제 객체 대신 가짜 객체를 사용하여 외부 의존성을 제거
        - FIRST원칙의 F, I, R을 지키기 위해 사용
    - Stub
        - 상태를 검증하기 위해 사용하는 가짜 객체
        - 의존 객체가 반환한 값을 기반으로 “테스트 대상이 올바른 결과를 냈는가?”를 검증할 때 사용
        - 동작 방식
            - SUT → Stub 호출 → 미리 정해둔 값 반환 → SUT 결과 확인
        - 검증 대상
            - 테스트 대상의 최종 상태
            - 반환값
            - 결과값
        
        → Stub은 원하는 상황을 만들어 주는 가짜 답변자.
        
    - Mock
        - 특정 메서드가 호출되었는지 감시하는 가짜 객체
        - “감시자” 역할
        - “의존 객체가 올바르게 호출되었는가?”를 검증할 때 사용
        - 동작방식
            - SUT 실행 → Mock 호출 → 호출 여부 기록 → verify로 검증
        
        ```java
        verify(emailClient, times(1))
            .sendWelcomeEmail(userEmail);
        ```
        
        → `sendWelcomeEmail()` 에서 `userEmail` 인자로 정확히 1번 호출되었는가?
        
        → Mock은 테스트 대상이 의존 객체와 어떻게 상호작용했는지 확인하는 감시자
        
        - Mock을 과도하게 사용하면 결과가 아닌 내부 구현 방식까지 검증하게 되어, 기능은 그대로인데 호출 구조만 바뀌어도 테스트가 깨져 리팩토링을 방해할 수 있다.
    - 상태 검증 : Stub 사용
        - 값을 반환 → 최종 결과 검증
        - 관심사 : 결과가 맞는가?
    - 행위 검증 : Mock 사용
        - 메서드 호출 감시 → verify
        - 관심사 : 제대로 호출했는가?
    - Stub vs Mock
    
    | 구분 | Stub | Mock |
    | --- | --- | --- |
    | 역할 | 답변자 | 감시자 |
    | 목적 | 상황 만들기 | 호출 검증 |
    | 검증 대상 | 반환값, 상태 | 메서드 호출 |
    | 주 사용 | 상태 검증 | 행위 검증 |
    | 대표 메서드 | when().thenReturn() | verify() |
    - 권장 방식
        - 대부분 상태 검증 중심
        - 꼭 필요한 외부 의존성만 Mock 사용
        - 이메일 발송, 결제 요청, 메시지 발행처럼 호출 자체가 중요한 경우에 Mock 사용
- JUnit
    - 자바에서 가장 많이 사용되는 테스트 프레임워크
    - 테스트를 작성하는 규칙, 실행 엔진, 검증 도구를 제공
    - 테스트의 성공과 실패를 사람이 아니라 JUnit이 자동으로 판단
    - 사용 이유
        - 테스트를 체계적으로 작성
        - 테스트를 자동으로 실행할 수 있다.
        - 결과를 명확하게 확인할 수 있다.
        - IDE, CI/CD와 연동하여 테스트 자동화를 구축할 수 있다.
    - 동작 흐름
        - 준비(Given) → 실행(When) → 검증(Then) → 성공 또는 실패
    - 주요 어노테이션
        - @Test
            - 테스트 메서드임을 JUnit에게 알려준다.
            - JUnit은 @Test가 붙은 메서드를 찾아 실행.
        - @DisplayName
            - 테스트 이름을 사람이 읽기 쉽게 지정
            - 테스트 결과 화면에서 표시
        - @BeforeEach
            - 각 테스트 실행 전에 매번 실행
            - 테스트 독립성을 위해 주로 객체 생성에 사용
        - @AfterEach
            - 각 테스트 실행 후 매번 실행
            - 리소스 정리 등에 사용
        - @BeforeAll
            - 테스트 클래스 전체에서 한 번만 실행
            - 공통 초기화 작업에 사용
        - @AfterAll
            - 테스트 클래스 전체가 끝난 후 한 번만 실행
            - 공통 자원 정리에 사용
    - 주요 Assertion
        - assertEquals
            - 기대값과 실제값이 같은지 검증
        - assertNotNull
            - 객체가 null이 아닌지 검증
        - assertThrows
            - 특정 예외가 발생하는지 검증
            - 단순히 예외 발생 여부뿐 아니라 예외 메시지까지 검증 가능
    - 테스트 작성 시 중요한 점
        - 성공 케이스만 테스트하지 않는다.
        - 예외 상황도 반드시 테스트한다.
        - 좋은 테스트는 정상 동작 + 예외 상황을 함께 검증
- Mockito
    - 테스트 더블을 쉽게 만들어주는 라이브러리
    - 가짜 객체 생성, 동작 설정(Stubbing), 호출 검증(Verification)을 편하게 할 수 있다.
    - 개발자가 직접 가짜 클래스를 만들 필요 없이 자동으로 테스트 더블을 생성
    - 사용 이유
        - 테스트 더블 생성 작업을 자동화
        - 외부 의존성(DB, API 등)을 격리할 수 있다.
        - 빠르고 안정적인 테스트를 작성할 수 있다.
        - 개발자가 비즈니스 로직 검증에 집중할 수 있게 해준다.
    - 흐름
        - 가짜 객체 생성 → 스터빙 → 테스트 대상 실행 → 검증(Assertion, Verification)
    - @Mock
        - Mockito가 가짜 객체를 자동 생성
        - 의존 객체를 대체할 때 사용
    - @InjectMocks
        - 테스트 대상 객체를 생성하면서 Mock 객체를 자동 주입
        
        ```java
        @InjectMocks
        private CarService carService;
        // 같은 의미
        CarService carService =
            new CarService(carRepository);
        ```
        
    - 테스트 흐름
        1. Mock 객체 생성
        
        ```java
        @Mock
        private CarRepository carRepository;
        ```
        
        - Mockito가 가짜 객체를 생성
        - 실제 DB와 연결되지 않는다.
        1. Stubbing(Given - 준비)
        
        ```java
        when(carRepository.findById("avante"))
            .thenReturn(Optional.of(expectedCar));
        ```
        
        - Mock 객체가 어떤 값을 반환할지 설정하는 작업
        - 테스트 실행 전 준비 단계
        - "findById("avante")가 호출되면 expectedCar를 반환해라"라고 미리 설정하는 것
        1. 테스트 대상 실행(When)
        
        ```java
        Car foundCar =
            carService.getCarDetails("avante");
        ```
        
        - 실제 테스트 대상(SUT)을 실행하는 단계
        1. 검증(Then)
        
        ```java
        assertEquals("Avante", foundCar.getName());
        ```
        
        → 결과값 검증(상태 검증)
        
        ```java
        verify(carRepository)
            .findById("avante");
        ```
        
        → 호출 여부 검증(행위 검증)
        
    - 자주 사용하는 기능
        - 반환값 설정
        
        ```java
        when(repository.findById("1"))
            .thenReturn(user);
        ```
        
        - 예외 발생 설정
        
        ```java
        when(repository.findById("1"))
            .thenThrow(new RuntimeException());
        ```
        
        - 호출 검증
        
        ```java
        verify(repository, times(1))
            .findById("1");
        ```
        
        - 인자 매칭
        
        ```java
        when(repository.findById(anyString()))
            .thenReturn(user);
        ```
        
        → 어떤 문자열이 와도 같은 값 반환
        
    - BDD Mockito
        - 기존 Mockito
        
        ```java
        when(repository.findById("1"))
            .thenReturn(user);
        ```
        
        - BDD Mockito
        
        ```java
        given(repository.findById("1"))
            .willReturn(user);
        ```
        
        → 기능은 동일
        
        → Given / When / Then 흐름을 더 읽기 쉽게 표현
        
- Factory Pattern
    - 객체 생성 책임을 호출부에서 분리하는 패턴
    - 어떤 객체를 만들지 결정하는 역할을 Factory가 담당
    - 해결하려는 문제
    
    ```java
    Payment payment = new CardPayment(...);
    ```
    
    → new가 여러 곳에 흩어지면 생성 규칙 변경 시 모두 수정해야 한다.
    
    → 생성 책임과 비즈니스 로직이 섞인다.
    
    - Factory 적용
    
    ```java
    Payment payment = paymentFactory.create(type);
    ```
    
    → Service는 객체 생성 방법을 모른다
    
    → Factory에게 필요한 객체를 요청만 한다
    
    - 사용 이유
        - 생성 규칙을 한 곳에 모을 수 있다.
        - 생성 방식이 바뀌어도 Factory만 수정하면 된다.
        - Service는 본래 역할에만 집중할 수 있다.
        - 테스트하기 쉬워진다.
    - 스프링에서
        - 스프링 컨테이너 = 거대한 Factory
            - 객체 생성
            - 의존성 주입
            - 생명주기 관리
            
            → 스프링이 대신 해준다.
            
            → 대부분 직접 Factory 만들 필요 없다.
            
    - Factory 직접 만드는 경우
        - 조건에 따라 구현체 선택
        - 객체 조립 과정이 복잡한 경우
        - 생성 규칙이 많은 경우
