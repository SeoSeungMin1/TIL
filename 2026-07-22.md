## 배운 내용

- RDS
    - AWS에서 제공하는 관계형 데이터베이스(RDB) 관리형 서비스
    - MySQL, PostgreSQL 등의 데이터베이스를 생성, 운영, 확장할 수 있다.
    - 사용 이유
        - DB 설치, 패치, 백업, 복구 등의 운영 작업을 AWS가 대신 관리하기 위해
        - 개발자는 DB 설계와 SQL 작성에 집중하기 위해
        - 장애 대응과 확장성을 쉽게 구성하기 위해
    - 역할
        - 관계형 데이터베이스 제공
        - 자동 백업 및 복구
        - 소프트웨어 패치 및 유지보수
        - Multi-Az, Read Replica 등을 통해 고가용성과 확장 지원
    - RDS 위치
        
        ```
        사용자
           │
        Route 53
           │
        ELB
           │
        EC2 (Spring Boot)
           │
        RDS Endpoint (DNS)
           │
        RDS
        ```
        
        → EC2는 RDS 엔드포인트(DNS 이름)로 접속
        
        → 엔드포인트가 실제 RDS 인스턴스 찾아 연결
        
    - RDS 엔드포인트
        - RDS는 고정 IP가 아닌 DNS 이름(Endpoint)으로 접속
        - RDS 인스턴스가 변경되어도 애플리케이션 설정 변경할 필요 없다.
    - Multi-AZ vs Read Replica
        
        
        | 구분 | Multi-AZ | Read Replica |
        | --- | --- | --- |
        | 목적 | 장애 대비 | 조회 부하 분산 |
        | 보조 DB 사용 | 평소 사용하지 않음 | 조회 요청 처리 |
        | 장애 발생 시 | 자동 장애 조치(Failover) | 일반적으로 자동 전환 아님 |
        | 핵심 | 고가용성 | 성능 향상 |
    - 요청 처리 흐름
        
        ```
        EC2 (Spring Boot)
                │
        RDS Endpoint(DNS)
                │
        RDS Security Group
                │
        RDS
        ```
        
        → EC2가 엔드포인트로 연결을 요청
        
        → RDS 보안 그룹이 연결을 허용하면 DB에 접속
        
        → DB 계정과 권한을 확인한 뒤 SQL 수행
        
    - 특징
        - 완전 관리형
        - 고정 IP 대신 Endpoint(DNS) 사용
        - 자동 백업 및 패치 지원
        - Multi-AZ로 장애 대응 가능
        - Read Replica로 조회 성능 향상 가능
- Simple Storage Service (S3)
    - AWS에서 제공하는 객체 스토리지 서비스
    - 이미지, 동영상, 로그, 백업 파일 등 비정형 데이터를 객체 단위로 저장
    - 사용 이유
        - 이미지, 파일 등을 DB 대신 저장하기 위해
        - 용량 걱정 없이 대용량 파일을 저장하기 위해
        - 높은 내구성과 가용성으로 안전하게 파일을 관리하기 위해
        - 파일 저장을 애플리케이션 서버와 분리하여 부담을 줄이기 위해
    - 역할
        - 파일 업로드 및 다운로드
        - 이미지, 동영상, 로그, 백업 파일 저장
        - 객체 단위 데이터 관리
    - S3 위치
        
        ```
        AWS 계정
           │
        서울 리전
           │
        ├── VPC
        │    ├── EC2
        │    └── RDS
        │
        └── S3
        ```
        
        → S3는 VPC 안에 있는 자원이 아니라 리전 단위 서비스
        
        → EC2는 S3 API(HTTPS API)를 호출하여 파일을 업로드하거나 다운로드
        
    - S3 구성 요소
        - Bucket
            - 객체를 저장하는 최상위 컨테이너
            - 리전 단위로 생성
            - 이름은 전 세계에서 유일해아 함
        - Object
            - S3에 저장되는 실제 파일
            - 파일데이터(실제 데이터)와 메타데이터(그 데이터를 설명하는 정보)를 함께 가진다.
        - Key
            - 버킷 안에서 객체를 식별하는 이름
            - images/profile.png 처럼 폴더처럼 보이지만 실제 폴더는 아니다.
    - 요청 처리 흐름
        
        ```
        사용자
           │
        EC2 (Spring Boot)
           │
        HTTPS API
           │
        S3 Bucket
           │
        Object(Key)
        ```
        
        → 애플리케이션은 S3 API를 호출하여 파일을 저장하거나 조회
        
        → 게시글 정보는 RDS, 파일은 S3, 파일 위치(URL, Object key)는 RDS에 문자열로 저장
        
    - RDS와 S3 차이
        
        
        | RDS | S3 |
        | --- | --- |
        | 관계형 데이터 저장 | 파일 저장 |
        | SQL 사용 | 객체(Object) 저장 |
        | VPC 내부에서 연결 | HTTPS API 호출 |
        | 보안 그룹으로 접근 제어 | IAM, Bucket Policy로 접근 제어 |
    - 특징
        - 객체 단위 저장
        - Bucket + Key로 객체를 식별
        - 높은 내구성과 가용성 제공
        - 용량 제한 없이 확장 가능
        - IAM과 Bucket Policy로 접근 권한 관리
- CloudFront
    - AWS에서 제공하는 CDN 서비스
        
        → CDN : 사용자와 가까운 서버에 콘텐츠의 사본(캐시)을 저장해 두고, 빠르게 전달하는 전 세계 분산 네트워크
        
    - 전 세계 엣지 로케이션에 콘텐츠를 캐시하여 사용자에게 빠르고 안전하게 전달한다.
        
        → 엣지 로케이션 : S3의 원본 데이터를 캐시하여 저장하고, 사용자에게 가장 가까운 위치에서 빠르게 제공하는 CloudFront의 데이터센터
        
    - 사용 이유
        - 사용자와 가까운 곳에서 콘텐츠를 제공하여 응답 속도를 높이기 위해
        - 원본 서버(S3, EC2 등)의 트래픽과 부하를 줄이기 위해
        - 전 세계 사용자에게 동일한 콘텐츠를 빠르게 제공하기 위해
    - CloudFront 위치
        
        ```
        사용자
           │
        CloudFront (Edge Location)
           │
        Origin (S3, EC2, ELB)
        ```
        
        → CloudFront는 글로벌 서비스로 전 세계 엣지 로케이션에서 동작
        
        → Origin은 특정 리전에 존재
        
    - 요청 처리 흐름
        
        ```
        사용자
           │
        CloudFront
           │
        Cache Hit ?
         ┌──┴──┐
         │     │
        Yes    No
         │      │
         ▼      ▼
        응답   Origin(S3)
                 │
                 ▼
              객체 저장 후 응답
        ```
        
        → Cache Hit : 엣지 로케이션에 캐시가 있으면 바로 응답
        
        → Cache Miss : 캐시가 없으면 Origin에서 가져와 사용자에게 전달하고, 엣지 로케이션에 캐시
        
    - Origin
        - CloudFront가 콘텐츠(S3 버킷안에 있는 객체)를 가져오는 원본 서버
        - Origin이 될 수 있는 대상
            
            → S3, EC2, ELB, 온프레미스 서버
            
    - 특징
        - CDN 서비스
        - 전 세계 엣지 로케이션에서 캐시 제공
        - Origin의 부하 감소
        - 응답 속도 향상
        - S3를 직접 공개하지 않고 CloudFront를 통해 접근 가능
