## 배운 내용

- VDOM
    - 실제 DOM을 직접 수정하기 전에, React가 메모리 안에 만들어 두는 가벼운 JavaScript 객체 트리
    - 현재 상태라면 화면이 어떤 모습이어야 하는지를 표현한 가상 화면 구조
    - 실제 DOM과 비슷한 구조를 가지지만, 브라우저 렌더링 시스템과 직접 연결되어 있지는 않다.
    - 사용 이유
        - 실제 DOM 조작을 최소화하기 위해
        - 변경된 부분만 찾아 실제 DOM에 반영하기 위해
        - 개발자가 DOM 수정 절차보다 “현재 상태에서 어떤 UI를 보여줄지”에 집중하도록 하기 위해
    - DOM과 VDOM 차이
        
        
        | 구분 | DOM | VDOM |
        | --- | --- | --- |
        | 위치 | 브라우저가 관리 | React가 메모리에서 관리 |
        | 형태 | HTML을 해석한 실제 트리 | JavaScript 객체 트리 |
        | 화면 연결 | 실제 화면과 연결됨 | 화면에 직접 보이지 않음 |
        | 수정 비용 | 비교적 무거움 | 가볍게 비교 가능 |
        | 역할 | 실제 화면 관리 | 변경 전후 비교용 중간 구조 |
    - 동작 방식
        - state 또는 props 변경 → 컴포넌트 다시 실행 → 새로운 VDOM 생성 → 이전 VDOM과 새로운 VDOM 비교 → 달라진 부분 찾기 → 필요한 부분만 실제 DOM에 반영
    - Diffing
        - 이전 VDOM과 새로운 VDOM을 비교해서 무엇이 달라졌는지 찾는 과정
        - React는 모든 경우를 완벽하게 비교하지 않고, 빠른 비교를 위해 몇 가지 규칙을 사용한다.
        - 주요 규칙
            1. 같은 위치의 요소 타입이 다르면 기존 트리를 버리고 새로 만든다.
            
            ```jsx
            <p>로그인이 필요합니다</p>
            <button>로그아웃</button>
            
            // p에서 button으로 타입이 바뀌었으므로 새로 만든다
            ```
            
            1. 요소 타입이 같으면 기존 노드는 유지하고 바뀐 속성이나 내용만 수정한다.
            
            ```jsx
            <p>클릭 횟수: 0</p>
            <p>클릭 횟수: 1</p>
            
            // p타입은 같으므로 텍스트만 수정한다
            ```
            
    - Patching
        - Diffing으로 찾은 변경 사항을 실제 DOM에 반영하는 과정
        - 전체 DOM을 다시 만드는 것이 아니라 필요한 부분만 수정한다.
    - Reconciliation
        - 상태 변경 후 새로운 VDOM을 만들고, 이전 VDOM과 비교한 뒤, 실제 DOM에 반영할 변경 사항을 결정하는 전체 과정
        - 쉽게 말해 React가 화면을 다시 맞추는 과정
        - 렌더 단계 : 새로운 VDOM을 만들고 이전 VDOM과 비교
        - 커밋 단계 : 비교 결과를 실제 DOM에 반영한다.
- Component
    - 입력(props)을 받아 현재 데이터에 맞는 UI 구조(JSX)를 반환하는 함수
    - 화면을 구성하는 독립적인 UI 조각
    - React 애플리케이션은 여러 컴포넌트를 조합하여 화면을 만든다.
    - 사용 이유
        - 재사용성을 높이기 위해
        - 유지보수를 쉽게 하기 위해
        - 화면을 역할별로 분리하여 관리하기 위해
        - 일관된 UI를 제공하기 위해
    - 기본 구조
        
        ```jsx
        function Hello() {
          return <h1>안녕하세요!</h1>;
        }
        
        <Hello />
        ```
        
        → 컴포넌트는 JSX를 반환해야 한다.
        
        → 컴포넌트 이름은 반드시 대문자로 시작해야 한다.
        
    - 특징
        - 캡슐화 : 내부 구현을 숨기고 props만 공개
        - 재사용성 : 같은 컴포넌트를 여러 곳에서 사용
        - 조합(composition) : 작은 컴포넌트를 조립해 큰 화면 구성
        - 단방향 데이터 흐름 : 데이터는 부모 → 자식으로 전달
    - Props
        - 부모가 자식 컴포넌트에게 전달하는 데이터
        - 읽기 전용
        - 자식은 props를 직접 수정할 수 없다.
    - 단방향 데이터 흐름
        - 부모 State → Props → 자식 컴포넌트
        - 데이터는 부모 → 자식 방향으로만 전달
        - 자식은 값을 직접 수정하지 못함
        - 값을 변경하려면 부모가 전달한 콜백 함수를 호출해야 함
    - 리스트 렌더링
        
        ```jsx
        <ul>
          {items.map(item => (
            <li key={item.id}>{item.name}</li>
          ))}
        </ul>
        ```
        
        → 배열 데이터를 반복하여 화면에 출력
        
        → React는 각 항목을 구분하기 위해 key를 사용
        
    - Key
        - React가 리스트 항목의 정체를 구분하기 위한 고유 식별자
        - VDOM Diffing 과정에서 사용됨
        - React는 key를 기준으로 이전 항목과 새로운 항목을 연결한다.
        - 사용 이유 : 어떤 항목이 유지되고 어떤 항목이 추가, 삭제되었는지 판단
    - index를 key로 쓰면 안되는 이유
        
        ```jsx
        items.map((item, index) => (
          <li key={index}>{item}</li>
        ))
        
        // 문제 -> 0 : 사과, 1 : 바나나, 2 : 체리
        // 딸기 추가 -> 0 : 딸기, 1 : 사과, 2 : 바나나, 3 : 체리
        ```
        
        → 모든 index가 밀린다.
        
        → React는 기존 항목이 이동한 것이 아니라 내용이 바뀌었다고 오해할 수 있음
        
        → 체크박스 상태, 입력값 등이 다른 항목에 붙을 수 있음
        
    - 올바른 key
        
        ```jsx
        const items = [
          { id: "a1", name: "사과" },
          { id: "b2", name: "바나나" },
          { id: "c3", name: "체리" }
        ];
        
        items.map(item => (
          <li key={item.id}>{item.name}</li>
        ))
        ```
        
        → 서버에서 내려주는 id 같은 고유값을 사용해야 한다.
        
- props
    - 부모 컴포넌트가 자식 컴포넌트에게 전달하는 데이터
    - 컴포넌트의 입력값(Input)
    - 컴포넌트 간 데이터를 전달하기 위한 읽기 전용 속성
    - 특징
        - 부모 → 자식 방향으로만 전달
        - 함수의 매개변수처럼 사용한다.
        - 읽기 전용이라 자식이 직접 수정할 수 없다.
    - 필요 이유
        1. 재사용성
            - 같은 컴포넌트를 여러 상황에서 사용할 수 있다.
            
            ```jsx
            <Button label="저장" color="green" />
            <Button label="삭제" color="red" />
            <Button label="취소" color="gray" />
            ```
            
            → Button 컴포넌트는 하나지만 props에 따라 다른 UI를 만든다
            
        2. 역할 분리
            - 부모는 데이터 관리, 자식은 화면 표시
            - 부모는 어떤 데이터를 보여줄지 결정
            - 자식은 받은 데이터를 어떻게 보여줄지만 담당
    - 기본 사용 방법
        - 부모
        
        ```jsx
        <ProductCard
          name="노트북"
          price={1500000}
          inStock={true}
        />
        ```
        
        - 자식
        
        ```jsx
        function ProductCard({ name, price, inStock }) {
          return (
            <article>
              <h2>{name}</h2>
              <p>{price}원</p>
              <p>{inStock ? "재고 있음" : "품절"}</p>
            </article>
          );
        }
        ```
        
        → 부모가 값을 전달하고 자식은 받아서 사용
        
    - 전달 가능한 값
        - 문자열
        
        ```jsx
        <Greeting name="지은" />
        ```
        
        - 숫자, 불린
        
        ```jsx
        <Product
          name="노트북"
          price={1500000}
          inStock={true}
        />
        ```
        
        - 객체
        
        ```jsx
        <TodoItem todo={item} />
        ```
        
        - 함수
        
        ```jsx
        <Button onClick={handleClick} />
        ```
        
        → props는 데이터뿐 아니라 함수도 전달 가능
        
    - 함수 Props
        
        ```jsx
        function Button({ onClick }) {
          return <button onClick={onClick}>클릭</button>;
        }
        ```
        
        → 자식은 이벤트를 발생시키고 실제 동작은 부모가 결정
        
        → 부모는 함수 전달, 자식은 함수 실행
        
    - 기본값
        
        ```jsx
        function Avatar({
          src,
          alt = "사용자 이미지"
        }) {
          return <img src={src} alt={alt} />;
        }
        ```
        
        → 부모가 값을 주지 않으면 기본값 사용
        
    - Children
        - 태그 사이의 내용은 자동으로 children props로 전달
        
        ```jsx
        <Card title="공지">
          <p>서버 점검이 있습니다.</p>
        </Card>
        
        function Card({ title, children }) {
          return (
            <div>
              <h3>{title}</h3>
              {children}
            </div>
          );
        }
        ```
        
        → children에 <p>서버 점검이 있습니다.</p> 들어감
        
        → 공통 레이아웃을 만들 때 자주 사용
        
    - Props vs State
        
        
        | 구분 | Props | State |
        | --- | --- | --- |
        | 소유자 | 부모 | 자기 자신 |
        | 수정 가능 여부 | 읽기 전용 | 직접 수정 가능 |
        | 역할 | 데이터 전달 | 상태 저장 |
        | 변경 주체 | 부모 | 현재 컴포넌트 |
    - Props Drilling
        - App → Page → Layout → Content → Button
        - 맨 위의 데이터를 맨 아래에 전달하기 위해 중간 컴포넌트들이 계속 props를 넘겨주는 현상
        - 컴포넌트 구조가 깊어질수록 불편해진다.
        - 해결 방법 : Context API, Zustand, Jotai, Redux
