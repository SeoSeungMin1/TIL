## 배운 내용

- this
    - 함수 안에서 자동으로 만들어지는 값
    - 이 값은 함수를 어떻게 호출했는지에 따라 달라진다.
    - 스코프는 선언 위치로 정해지고, this는 호출 방식으로 정해진다.
    - 사용 이유
        - 같은 함수를 여러 객체가 함께 사용할 수 있게 하기 위해
        - 함수 안에서 특정 객체 이름을  직접 적지 않고, 이 함수를 호출한 객체를 가리킬 수 있다.
        
        ```jsx
        const user = {
          name: "Kim",
          getName() {
            return this.name; // this는 user를 가리킴.
          },
        };
        
        user.getName(); // Kim
        ```
        
    - 바인딩 : 어떤 이름과 실제 값을 연결하는 것
    - this 바인딩 종류
        
        
        | 호출 방식 | this |
        | --- | --- |
        | 일반 함수 호출 (기본 바인딩) | 전역 객체 / strict mode에서는 undefined |
        | 객체 메소드 호출 (암시적 바인딩) | 점 왼쪽 객체 |
        | call, apply, bind (명시적 바인딩) | 직접 지정한 객체 |
        | new 호출 (new 바인딩) | 새로 생성된 객체 |
        | 화살표 함수 | 자기 this 없음, 바깥 this 사용 |
        
        → 우선 순위 : new > 명시적 > 암시적 > 기본 바인딩
        
    - 핵심 규칙
        
        ```jsx
        obj.getName();
        ```
        
        → 점 왼쪽 객체인 `obj` 가 this가 된다.
        
        ```jsx
        obj.getName.call(other);
        ```
        
        → `call` 로 직접 지정했기 때문에 this는 `other` 가 된다.
        
        ```jsx
        const userGreet = greet.bind(user);
        ```
        
        → `bind` 는 this가 `user` 로 고정된 새 함수를 만든다.
        
    - 화살표 함수 this
        - 화살표 함수는 자기만의 this를 가지지 않는다.
        
        ```jsx
        const obj = {
          name: "Kim",
          getName: () => {
            return this.name;
          },
        };
        ```
        
        → this는obj가 아니라 바깥 스코프의 this를 따른다.
        
        → 객체 메소드는 보통 화살표 함수보다 일반 함수로 작성하는게 안전.
        
- 클로저
    - 함수가 자신이 만들어진 스코프(렉시컬 환경)를 기억해서, 그 스코프 밖에서 실행되더라도 해당 변수에 계속 접근할 수 있게 해주는 성질
    - 바깥 함수의 실행이 끝난 뒤에도 내부 함수가 참조하는 변수는 메모리에 유지된다.
    - 클로저는 렉시컬 스코프(함수가 선언된 위치 기준으로 스코프를 결정하는 규칙)를 기반으로 동작한다.
    - 사용 이유
        - 전역 변수 사용을 줄이기 위해
        - 데이터를 외부에서 직접 접근하지 못하게 보호하기 위해(캡슐화)
        - 함수마다 독립적인 상태를 유지하기 위해
        - 설정값이나 상태를 기억하는 전용 함수를 만들기 위해
    - 동작 방식
        - 함수가 생성될 때 자신이 접근 가능한 변수들을 함께 기억한다.
        - 반환된 함수가 그 변수를 계속 참조하면 함수 실행이 끝나도 해당 변수는 제거되지 않는다.
        - 클로저는 값을 복사하는 것이 아니라 변수 자체를 참조한다.
        - 함수를 반환하는 순간 클로저가 만들어지는 것이 아니라, 함수가 외부 스코프의 변수를 참조하는 순간 클로저가 형성
    - 특징
        - 클로저마다 독립적인 상태를 가진다.
        
        ```jsx
        function createCounter() {
            let count = 0;
        
            return function () {
                count++;
                return count;
            };
        }
        
        const a = createCounter();
        const b = createCounter();
        
        console.log(a()); // 1
        console.log(a()); // 2
        console.log(b()); // 1
        ```
        
        → a와 b는 같은 함수에서 만들어졌지만 서로 다른 count를 기억한다.
        
    - 캡슐화 예시
        
        ```jsx
        function createAccount(initial) {
            let balance = initial;
        
            return {
                deposit(amount) {
                    balance += amount;
                },
                getBalance() {
                    return balance;
                }
            };
        }
        
        console.log(account.balance);
        // undefined
        ```
        
        → account.deposit(...), account.getBalance() 이 두 함수만 `balance` 에 접근 가능
        
        → balance는 외부에서 직접 접근할 수 없고, 제공된 메서드를 통해서만 사용할 수 있다.
        
    - 주의 사항
        - 클로저는 변수를 계속 참조하기 때문에 메모리에 유지된다.
        - 불필요한 클로저를 오래 보관하면 메모리 사용량이 증가할 수 있다.
        - 반복문에서 var와 함께 사용하면 의도하지 않은 값이 참조될 수 있으므로 let 사용이 권장된다.
        
        ```jsx
        for (var i = 1; i <= 3; i++) {
            setTimeout(function () {
                console.log(i);
            }, 0);
        }
        ```
        
        → 결과 : 4 4 4
        
        → 모든 콜백이 같은 i 변수를 참조하기 때문에
        
- 프로토타입
    - 객체가 새로 만들어질 때 기본적으로 물려받는 속성과 메서드의 집합
    - 객체에 없는 속성이나 메서드를 찾을 때 참조하는 공유 공간
    - 자바스크립트의 상속은 프로토타입을 기반으로 동작
    - 사용 이유
        - 여러 객체가 공통 기능을 공유하여 메모리를 효율적으로 사용하기 위해
        - 객체 간에 공통 기능을 재사용하기 위해
        - 상속 구조를 구현하기 위해
    - 동작 방식
        - 객체마다 다른 값은 객체 자신(this)에 저장
        - 여러 객체가 공통으로 사용하는 기능은 prototype에 한 번만 저장
        - 객체에서 속성이나 메서드를 찾을 때
            1. 객체 자기 자신 확인
            2. 없으면 프로토타입 확인
            3. 없으면 프로토타입의 프로토타입 확인
            4. 끝까지 없으면 undefined 반환
    - 프로토타입 공유 예시
    
    ```jsx
    function User(name) {
        this.name = name;
    }
    
    User.prototype.greet = function () {
        console.log(this.name + "님 안녕하세요");
    };
    
    const a = new User("지민");
    const b = new User("현우");
    
    console.log(a.greet === b.greet); // true
    ```
    
    → greet 함수는 하나만 존재하고 모든 객체가 공유
    
    - 프로토타입 체인
    
    ```jsx
    function House() {
        this.tv = "samsung";
    }
    
    House.prototype.laptop = "LG";
    
    const wayne = new House();
    
    console.log(wayne.laptop);
    ```
    
    → 동작 순서 : wayne 객체 확인 → laptop 없음 → House.prototype 확인 → laptop 발견 → “LG” 반환
    
    → 객체에 없는 값을 프로토타입을 따라 위로 올라가며 찾는 과정을 프로토타입 체인
    
    - 프러퍼티 우선순위
    
    ```jsx
    const tom = new House();
    
    tom.laptop = "apple";
    
    console.log(tom.laptop); // apple
    ```
    
    → 객체 자신에게 같은 이름의 속성이 있으면 프로토타입까지 올라가지 않는다.
    
    - hasOwnProperty()
    
    ```jsx
    console.log(wayne.hasOwnProperty("tv"));     // true
    console.log(wayne.hasOwnProperty("laptop")); // false
    ```
    
    → 해당 속성이 객체 자신에게 있는지 확인하는 메서드
    
    - 내장 객체도 프로토타입 사용
    
    ```jsx
    const list = [1, 2, 3];
    
    console.log(list.includes(2));
    console.log(list.map(x => x * 2));
    ```
    
    → includes, map, push 등은 Array.prototype에 존재하는 메서드
    
    ```jsx
    console.log(list.hasOwnProperty("length"));   // true
    console.log(list.hasOwnProperty("includes")); // false
    ```
    
    → length는 배열 자신이 가지고 있고, includes는 프로토타입에서 빌려 쓴다.
    
    - 주의 사항
        - 내장 객체(Array.prototype, Object.prototype 등)를 직접 수정하는 것은 권장되지 않는다.
        - 다른 라이브러리나 코드와 충돌할 수 있다.
        - 객체마다 다른 데이터는 this에, 공통 기능은 prototype에 두는 것이 좋다.
- 클래스
    - 관련된 속성과 메서드를 묶어서 객체를 생성하기 위한 템플릿
    - 자바스크립트의 클래스는 새로운 상속 방식이 아니라 프로토타입 기반 상속을 더 쉽게 사용할 수 있도록 만든 문법
    - 내부적으로 프로토타입을 사용하여 동작
    - 사용 이유
        - 객체 생성 코드를 직관적으로 작성하기 위해
        - 관련된 속성과 메서드를 한곳에 모아 가독성을 높이기 위해
        - 상속 구조를 명확하게 표현하여 재사용성과 유지보수성을 높이기 위해
    - 사용 방법
    
    ```jsx
    class Car {
        constructor(model) {
            this.model = model;
        }
    
        drive() {
            console.log(`${this.model} is driving`);
        }
    }
    
    const myCar = new Car("Model A");
    myCar.drive();
    ```
    
    - 상속
    
    ```jsx
    class Bus extends Car {
        constructor(model) {
            super(model);
            this.speedLimit = 90;
        }
    
        applySpeedLimit() {
            console.log(`${this.model} : ${this.speedLimit}`);
        }
    }
    ```
    
    → extends로 부모 클래스 상속
    
    → super()로 부모 생성자를 호출
    
    - 클래스와 프로토타입 관계
    
    ```jsx
    class Car {
        drive() {}
    }
    ```
    
    ```jsx
    function Car() {}
    
    Car.prototype.drive = function () {};
    ```
    
    → 클래스는 프로토타입 위에 만들어진 문법
    
    → 기능이 새로 생긴 것이 아니라 프로토타입을 더 읽기 쉽게 작성하도록 도와준다.
    
    - 클래스 특징
        - 생성자는 반드시 new와 함께 호출해야 한다.
        - 클래스 내부는 자동으로 strict mode가 적용
        - 클래스 메서드는 프로토타입에 저장
        - 클래스 메서드는 열거되지 않는다.
    - strict mode
        - 자바스크립트가 위험하거나 애매한 문법을 더 엄격하게 검사하도록 만드는 모드
        - 사용 방법
        
        ```jsx
        "use strict";
        
        x = 10; // 오류
        ```
        
    - 프로토타입 방식과 차이점
        - 동작 원리는 동일하다.
        - 클래스는 생성자, 메서드, 상속 구조를 한 곳에서 볼 수 있어 가독성이 높다.
        - `extends`, `super` 문법으로 상속 관계를 명확하게 표현할 수 있다.
        - `new` 강제, strict mode 적용 등 안전장치가 추가되어 있다.
- React
    - 컴포넌트 단위로 사용자 UI를 선언적으로 구축할 수 있는 자바스크립트 라이브러리
    - 상태를 기반으로 화면을 관리하며, 상태가 변경되면 React가 필요한 부분만 다시 렌더링.
    - 내부적으로 Virtual DOM을 사용하여 화면을 효율적으로 갱신
    - 사용이유
        - 복잡한 UI를 구조적으로 관리하기 위해
        - 컴포넌트 재사용성을 높이기 위해
        - 상태와 화면을 연결하여 유지보수를 쉽게 하기 위해
        - 개발자가 DOM을 직접 조작하지 않고 UI 결과에 집중할 수 있게 하기 위해
    - DOM(Document Object Model)
        - 브라우저가 HTML 문서를 읽어서 메모리 안에 만든 **객체 형태의 트리 구조**
        - 자바스크립트는 DOM을 통해 HTML 요소를 읽고 수정할 수 있다.
    - 핵심 개념
        - 컴포넌트
            - 화면을 구성하는 독립적인 UI 조각
            - 함수 형태로 작성하며 화면 일부를 반환한다.
            - 여러 곳에서 재사용 가능하다.
            
            ```jsx
            function Button() {
              return <button>클릭</button>;
            }
            ```
            
        - 상태
            - 화면에 표시되는 데이터를 저장하는 값
            - 상태가 변경되면 React가 화면을 다시 계산한다.
            
            ```jsx
            const [count, setCount] = useState(0);
            ```
            
        - 선언적 UI
            - "어떻게 그릴지"가 아니라 "무엇을 보여줄지"를 작성하는 방식
            - 화면 갱신 과정은 React가 처리한다.
            
            ```jsx
            <p>Count: {count}</p>
            ```
            
    - Vanilla JS와 React의 차이
        
        
        | 구분 | Vanilla JS (명령형) | React (선언적) |
        | --- | --- | --- |
        | 개발 방식 | 어떻게 화면을 바꿀지 직접 작성 | 어떤 화면을 보여줄지만 작성 |
        | 데이터 변경 | 개발자가 직접 변경 | 상태(State) 변경 |
        | 화면 갱신 | 개발자가 직접 DOM 수정 | React가 자동 처리 |
        | DOM 접근 | `getElementById()`, `querySelector()` 등으로 직접 접근 | 직접 접근 거의 없음 |
        | 코드 예시 | `countElement.textContent = ...` | `setCount(...)` |
        | 관심사 | 데이터 변경 + 화면 변경 둘 다 신경 써야 함 | 상태만 신경 쓰면 됨 |
        | 유지보수 | 화면이 복잡해질수록 어려워짐 | 상태 중심이라 비교적 쉬움 |
        | 재사용성 | 직접 구조 설계 필요 | 컴포넌트 단위로 재사용 가능 |
    - 동작 방식
        - 버튼 클릭 → 상태 변경 → 컴포넌트 재실행 →새로운 UI 계산 → 변경된 부분만 실제 DOM 반영
    - 특징
        
        1. 컴포넌트 기반 아키텍처
        
        - 화면을 작은 조각으로 분리하여 개발
        - 재사용성과 유지보수성 향상
        
        2. 단방향 데이터 흐름
        
        - 데이터가 부모 → 자식 방향으로만 전달
        - 데이터 변경 위치를 추적하기 쉽다.
        
        3. Virtual DOM
        
        - 메모리 상의 가상 DOM 생성
        - 이전 화면과 새 화면을 비교
        - 변경된 부분만 실제 DOM에 반영
    - 등장 배경
        - Vanilla JS : DOM 직접 조작
        - jQuery : DOM 조작 간소화
        - Backbone : 데이터와 화면 분리 시도
        - Knockout.js : 상태 기반 UI 갱신
        - AngularJS : 강력한 프레임워크 제공
        - React : 컴포넌트, 단방향 데이터 흐름, Virtual DOM으로 문제 해결
    - React의 핵심 가치
        - 상태(State)를 기반으로 UI를 선언적으로 작성한다.
        - 개발자는 화면의 결과만 정의한다.
        - 실제 DOM 조작과 화면 갱신은 React가 처리한다.
- JSX
    - JavaScript 안에서 HTML처럼 UI를 작성할 수 있게 해주는 문법 확장
    - HTML처럼 보이지만 실제로는 JavaScript 코드이다.
    - 브라우저는 JSX를 직접 이해하지 못하며, 빌드 과정에서 일반 JavaScript로 변환된다.
    - 사용 이유
        - UI 구조와 데이터를 한 곳에서 직관적으로 작성하기 위해
        - React 컴포넌트를 읽고 작성하기 쉽게 만들기 위해
        - 복잡한 함수 호출을 HTML 형태로 간결하게 표현하기 위해
    - 동작 방식
        - JSX
        
        ```jsx
        const Greeting = () => <h1>안녕하세요</h1>;
        ```
        
        - Babel이나 SWC가 변환
        
        ```jsx
        const Greeting = () =>
          React.createElement("h1", null, "안녕하세요");
        ```
        
        → JSX 태그는 HTML이 아니라 함수 호출 코드로 변환
        
    - JSX 표현식
        - `{}` 안에는 JavaScript 표현식을 넣을 수 있다.
        - 값으로 평가되는 것만 사용 가능
        - 가능
        
        ```jsx
        <p>{a + b}</p>
        <p>{user.name}</p>
        <p>{formatDate()}</p>
        ```
        
        - 불가능
        
        ```jsx
        <p>{if (a > b) {}}</p>
        <p>{for (...) {}}</p>
        ```
        
        → if, for는 문장이라 사용할 수 없다.
        
    - 조건부 렌더링
        - 삼항 연산자
        
        ```jsx
        <p>{a > b ? "a" : "b"}</p>
        ```
        
        - 논리 연산자
        
        ```jsx
        {isLogin && <p>로그인 상태</p>}
        ```
        
        - 주의
        
        ```jsx
        {0 && <p>Hello</p>}
        ```
        
        → 화면에 0이 출력될 수 있다.
        
    - 반복 렌더링
        
        ```jsx
        {list.map((item) => (
          <li key={item}>{item}</li>
        ))}
        ```
        
        → 배열을 화면 요소로 변환할 때 map 사용
        
    - 속성 이름
    
    | HTML | JSX |
    | --- | --- |
    | class | className |
    | for | htmlFor |
    | tabindex | tabIndex |
    
    → JSX는 DOM Property 이름 사용
    
    → DOM Property : 브라우저가 HTML 요소를 자바스크립트 객체로 만들었을 때 그 객체가 가지는 속성 이름
    
    - JSX와 빌드
        - JSX 작성 → Babel/SWC 변환 → 일반 JavaScript 코드 생성 → 브라우저 실행
        - 브라우저는 JSX가 아니라 변환된 JavaScript를 실행한다.
    - 장점
        - UI 구조를 직관적으로 표현 가능
        - HTML과 JavaScript를 한 곳에서 관리 가능
        - React 컴포넌트 작성이 쉬워짐
        - 컴포넌트 재사용이 편리
        - 빌드 과정에서 최적화 가능
    - 단점
        - Babel, SWC, Vite, Webpack 등의 빌드 도구가 필요
        - UI 코드와 비즈니스 로직이 한 파일에 섞일 수 있음
        - React 문법을 처음 배울 때 진입장벽이 존재
    - React와 JSX 관계
        - React는 JSX 없이도 사용 가능하다
        - 하지만 JSX를 사용하면 UI를 훨씬 읽기 쉽고 간결하게 작성할 수 있다
        - JSX는 React를 위한 필수 기능이 아니라 문법적 설탕이다.
